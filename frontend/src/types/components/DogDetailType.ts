export type DogFileSectionType = {
  files: string[]
  name: string
  sex: string
  neutralized: boolean
  kind: string
  hit: number
  age: number
}
export type ShelterNameType = {
  userName: string
  address: string
  // userLiked: boolean
  dogNo: number
}

export type DogAdditionalInfoType = {
  note: string
}

export type ShelterInfoSectionType = {
  address: string
  createdAt: string
  userName: string
}
