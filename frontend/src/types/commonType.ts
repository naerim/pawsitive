export type MainColorMoveCardType = {
  title: string
  subTitle: string
  url: string
}

export type LightColorMoveCardType = {
  title: string
  url: string
}

export type CloseFunctionType = {
  onClose: () => void
}
