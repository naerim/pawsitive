export type MainColorMoveCardType = {
  title: string
  subTitle: string
  url: string
}

export type LightColorMoveCardType = {
  title: string
  url: string
}

export type ProgressBarType = {
  currentStage: number
}
